import 'package:flutter/material.dart';

class ViewSinglePdfScreen extends StatelessWidget {
  const ViewSinglePdfScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Replace with actual PDF viewer widget
    return Scaffold(
      appBar: AppBar(
        title: Text('View PDF'),
      ),
      body: Center(
        child: Text('PDF content goes here'),
      ),
    );
  }
}
